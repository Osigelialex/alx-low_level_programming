#include "main.h"

/**
 * _islower -  function that checks for lowercase character
 * c - this is a parameter
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
