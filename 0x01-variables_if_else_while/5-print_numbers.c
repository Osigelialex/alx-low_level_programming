#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char numbers[10];
	int i;

	numbers = "0123456789";

	for (i = 0; i < 10; ++i)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}


	
